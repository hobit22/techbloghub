# Batch Module - Production Environment Configuration
app.crawler.enabled=${CRAWLER_ENABLED}
app.crawler.schedule.all-blogs=${CRAWLER_SCHEDULE_ALL_BLOGS}
app.crawler.schedule.needed-blogs=${CRAWLER_SCHEDULE_NEEDED_BLOGS}
app.crawler.schedule.stats=${CRAWLER_SCHEDULE_STATS}
app.crawler.stats.enabled=${CRAWLER_STATS_ENABLED}
app.crawler.http.timeout-seconds=${CRAWLER_TIMEOUT_SECONDS}
app.crawler.http.user-agent=${CRAWLER_USER_AGENT}
app.crawler.http.max-posts-per-feed=${CRAWLER_MAX_POSTS_PER_FEED}